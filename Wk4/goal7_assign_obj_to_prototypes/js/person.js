/**
 * Created by the JavaScript Development Team
 * Nambi Hawkins
 * Class: PWA
 * Goal: Goal7
 */
//window.Person=Person;
//create 2 array variables, jobs and actions. Jobs should hold 4 or more values and actions will be the actions a person can perform, for example Person.jobs = [“vet”, “doctor”, “lawyer”, “gm”], Person.actions = [“sleep”, “eat”, “work”];
//name will be selected in main.js
//action and job will be selected using the math.random() method and will be set in rows with the name
//update() function will be put in //main.js will be using the Person object, add to window
//place to change the action of the person, coinciding with the update() function in main.js for the name update
